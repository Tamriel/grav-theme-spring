<div class="modular-row callout">
    {% set image = page.media.images|first %}
    {% if image and not header.no_callout %}
        {{ image.lightbox(1024,768).cropResize(200,200).html('','','align-'~page.header.image_align) }}
    {% endif %}
{{ content }}
</div>
